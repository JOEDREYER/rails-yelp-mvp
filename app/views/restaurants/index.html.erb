
<h1>Restaurants Index Page</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%=link_to restaurant.name, restaurant_path(restaurant) %> - <%= restaurant.category %></li>
  <% end %>
</ul>

<div class="btn btn-dark"> <%=link_to "Create a New restaurant", new_restaurant_path %></div>
